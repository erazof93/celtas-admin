import { PagePlaceholder } from '@/components/ui/PagePlaceholder'

/**
 * Cupones — MÓDULO 6. Listado, generación manual y cupones por usuario.
 */
export default function CouponsPage() {
  return (
    <PagePlaceholder
      title="Cupones"
      description="La generación y gestión de cupones llegará en el módulo 6."
    />
  )
}
