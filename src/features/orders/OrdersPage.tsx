import { PagePlaceholder } from '@/components/ui/PagePlaceholder'

/**
 * Pedidos — MÓDULO 5. Listado, detalle y cambios de estado.
 */
export default function OrdersPage() {
  return (
    <PagePlaceholder
      title="Pedidos"
      description="El listado de pedidos, su detalle y los cambios de estado llegarán en el módulo 5."
    />
  )
}
