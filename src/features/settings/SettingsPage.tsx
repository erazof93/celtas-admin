import { PagePlaceholder } from '@/components/ui/PagePlaceholder'

/**
 * Configuración — MÓDULO 8. WhatsApp del negocio y roles de usuario.
 */
export default function SettingsPage() {
  return (
    <PagePlaceholder
      title="Configuración"
      description="La configuración del negocio y la gestión de roles llegará en el módulo 8."
    />
  )
}
