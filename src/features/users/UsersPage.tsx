import { PagePlaceholder } from '@/components/ui/PagePlaceholder'

/**
 * Usuarios — MÓDULO 9. Listado paginado de clientes y sus perfiles.
 */
export default function UsersPage() {
  return (
    <PagePlaceholder
      title="Usuarios"
      description="El listado de usuarios y sus perfiles llegará en el módulo 9."
    />
  )
}
